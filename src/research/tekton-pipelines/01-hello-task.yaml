apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: hello
spec:
  steps:
    - name: say-hello
      image: alpine:3
      script: |
        echo "Hello, Tekton!"
        echo "This step runs inside an alpine container as a Kubernetes Pod."
        echo "Current date: $(date)"
        echo "Hostname: $(hostname)"
