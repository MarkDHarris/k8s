apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - task.yaml
  - pipeline.yaml

commonLabels:
  app.kubernetes.io/part-of: app-ci
  app.kubernetes.io/managed-by: kustomize
